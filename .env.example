# 瞬语 AI STT 服务配置
# 复制此文件为 .env 并根据需要修改

# ============================================================================
# GPU 配置
# ============================================================================

# 启用 GPU 加速（1=启用，0=禁用，默认禁用）
SHUNYU_USE_GPU=1

# GPU 设备 ID（默认 0，多卡环境可指定其他显卡）
# 例如：0, 1, 2... 对应第1、2、3张显卡
SHUNYU_GPU_DEVICE_ID=0

# ============================================================================
# 说明
# ============================================================================
# 
# 启用 GPU 需要：
# 1. 安装 NVIDIA 显卡驱动
# 2. 安装 CUDA（推荐 11.8 或 12.x）
# 3. 安装 onnxruntime-gpu：pip install onnxruntime-gpu
#
# 启用后预期性能提升：
# - CPU 模式：约 500-1000ms 延迟
# - GPU 模式：约 100-200ms 延迟（5-10倍加速）
#
# 如果启用 GPU 后出现问题，会自动回退到 CPU 模式
